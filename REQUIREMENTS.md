# Project Requirements

- `beautiful soup`
- `pywebview`
- `pandoc` (OS Install)
- `pypandoc`
- `scrapy`

```
$ sudo dnf install pandoc

$ python3 -m venv .venv

$ source .venv/bin/activate

$ pip install requirements.txt
```
